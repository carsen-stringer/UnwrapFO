function  [xprop,pmax,dat,hyp]       = OptProb5d(dat,hyp)

[dat,hyp]        = ProbHypOpt(dat,hyp); 
if hyp.isGPU
    dat.P = gather(dat.P);
end
p                = dat.P;

[pmax,imax ]     = max(p(:));
xprop            = dat.rp(:,imax);
